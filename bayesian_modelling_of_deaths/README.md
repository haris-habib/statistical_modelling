# Bayesian Hierarchical & Pooled Modeling

This repository contains a full Python implementation of pooled and hierarchical
Bayesian mortality modeling using Gibbs sampling and classical t-based Bayesian inference.

## Contents
- `bayesian_models.py` — Main module containing:
  - Data construction
  - Pooled Bayesian model
  - Hierarchical Bayesian model with Gibbs sampling
  - Predictive sampling
- `requirements.txt`
- `description.txt`

## Running the Model
```bash
python bayesian_models.py
```

## Requirements
Install dependencies using:
```bash
pip install -r requirements.txt
```

